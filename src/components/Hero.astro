---

---

<section class="hero">
    <div class="container">
        <div class="content">
            <span class="badge">Developing games with kids</span>
            <h1>
                The Father-Son Game Studio: <span class="highlight"
                    >Building Worlds</span
                >, Growing Together
            </h1>
            <p>
                We are a father-son duo crafting fun games and sharing resources
                for parents who want to dive into game development with their
                children.
            </p>
            <div class="actions">
                <a href="/projects" class="btn btn-primary">See Our Games</a>
                <a href="/blog" class="btn btn-secondary">Read the Blog</a>
            </div>
        </div>
        <div class="image-wrapper">
            <div class="avatar-card">
                <img src="/avatar.jpg" alt="Sr. Jr. Games Original Avatar" />
            </div>
        </div>
    </div>
</section>

<style>
    .hero {
        padding: 8rem 0;
        position: relative;
        overflow: hidden;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 2rem;
        display: grid;
        grid-template-columns: 1.2fr 0.8fr;
        gap: 4rem;
        align-items: center;
    }

    .badge {
        display: inline-block;
        background: var(--color-navy);
        color: var(--color-white);
        padding: 0.4rem 1rem;
        border-radius: var(--radius-sm);
        font-size: 0.9rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
    }

    h1 {
        font-size: 4.5rem;
        line-height: 1.1;
        margin-bottom: 2rem;
    }

    .highlight {
        color: var(--color-red);
        background: linear-gradient(
            180deg,
            transparent 70%,
            var(--color-yellow) 70%
        );
    }

    p {
        font-size: 1.25rem;
        line-height: 1.6;
        color: var(--color-text);
        margin-bottom: 3rem;
        max-width: 600px;
    }

    .actions {
        display: flex;
        gap: 1.5rem;
    }

    .btn {
        padding: 1.2rem 2.5rem;
        border-radius: var(--radius-md);
        font-size: 1.1rem;
        font-weight: 800;
        font-family: var(--font-accent);
        text-align: center;
        border: 3px solid var(--color-navy);
        transition: all 0.2s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }

    .btn-primary {
        background-color: var(--color-yellow);
        color: var(--color-navy);
        box-shadow: 6px 6px 0 var(--color-navy);
    }

    .btn-primary:hover {
        transform: translate(-3px, -3px);
        box-shadow: 9px 9px 0 var(--color-navy);
    }

    .btn-secondary {
        background-color: var(--color-white);
        color: var(--color-navy);
        box-shadow: 6px 6px 0 var(--color-navy);
    }

    .btn-secondary:hover {
        transform: translate(-3px, -3px);
        box-shadow: 9px 9px 0 var(--color-navy);
    }

    .image-wrapper {
        position: relative;
    }

    .avatar-card {
        background: var(--color-white);
        padding: 1.5rem;
        border-radius: var(--radius-lg);
        border: 3px solid var(--color-navy);
        box-shadow: 12px 12px 0 var(--color-navy);
        transform: rotate(3deg);
        transition: transform 0.3s ease;
    }

    .avatar-card:hover {
        transform: rotate(0deg) scale(1.05);
    }

    .avatar-card img {
        width: 100%;
        height: auto;
        border-radius: var(--radius-md);
        display: block;
    }

    @media (max-width: 968px) {
        .container {
            grid-template-columns: 1fr;
            text-align: center;
        }

        .content {
            order: 2;
        }

        .image-wrapper {
            order: 1;
            max-width: 400px;
            margin: 0 auto;
        }

        h1 {
            font-size: 3rem;
        }

        .actions {
            justify-content: center;
        }
    }
</style>
