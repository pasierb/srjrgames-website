---

---

<section class="kofi-section">
    <div class="container">
        <div class="kofi-card">
            <header class="section-header">
                <span class="badge">Support Us</span>
                <h2>
                    Want to support our <span class="highlight">mission?</span>
                </h2>
                <p>
                    If you'd like to support our journey, consider buying us a
                    coffee! Every bit helps us keep creating.
                </p>
            </header>
            <div class="iframe-container">
                <iframe
                    id="kofiframe"
                    src="https://ko-fi.com/srjrgames/?hidefeed=true&widget=true&embed=true&preview=true"
                    style="border:none;width:100%;padding:4px;background:#f9f9f9;"
                    height="712"
                    title="srjrgames"></iframe>
            </div>
        </div>
    </div>
</section>

<style>
    .kofi-section {
        padding: 4rem 0;
        background: var(--color-white);
    }

    .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 0 2rem;
    }

    .kofi-card {
        background: var(--color-white);
        border: 3px solid var(--color-navy);
        border-radius: var(--radius-lg);
        box-shadow: 12px 12px 0 var(--color-navy);
        padding: 3rem;
        overflow: hidden;
    }

    .section-header {
        text-align: center;
        margin-bottom: 2rem;
    }

    .badge {
        display: inline-block;
        background: var(--color-navy);
        color: var(--color-white);
        padding: 0.4rem 1rem;
        border-radius: var(--radius-sm);
        font-size: 0.9rem;
        font-weight: 600;
        margin-bottom: 1rem;
    }

    h2 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        line-height: 1.1;
    }

    .highlight {
        color: var(--color-red);
        background: linear-gradient(
            180deg,
            transparent 70%,
            var(--color-yellow) 70%
        );
    }

    p {
        font-size: 1.15rem;
        color: var(--color-text);
        max-width: 600px;
        margin: 0 auto;
    }

    .iframe-container {
        border-top: 2px dashed var(--color-navy);
        padding-top: 2rem;
        margin-top: 2rem;
    }

    @media (max-width: 768px) {
        .kofi-card {
            padding: 1.5rem;
            box-shadow: 8px 8px 0 var(--color-navy);
        }

        h2 {
            font-size: 2rem;
        }

        .kofi-section {
            padding: 2rem 0;
        }
    }
</style>
